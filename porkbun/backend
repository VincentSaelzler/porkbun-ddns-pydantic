from typing import Literal, Protocol




BackendType = Literal["mock", "http"]

class Backend(Protocol):
    def post_request(self, request_type: RequestType) -> bytes: ...


_backends: dict[BackendType, Backend] = {
    "mock": porkbun.mockapi,
    "http": porkbun.httpapi,
}

def 




def post_request(request_type: RequestType) -> bytes: ...

def _post(url: str, json: dict[str, Any]):
    r = requests.post(url, json=json)
    r.raise_for_status()



def get_public_ip():
    print("hmm")
    return "x"
